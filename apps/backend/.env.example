# ============================================================================
# HARNESS MES Backend 환경변수 설정
# ============================================================================
# 이 파일을 .env로 복사하여 실제 값으로 변경하세요.
# .env 파일은 절대 Git에 커밋하지 마세요!
# ============================================================================

# =========================
# 애플리케이션 설정
# =========================
NODE_ENV=development
PORT=4000
API_PREFIX=api/v1

# =========================
# 데이터베이스 설정 (Supabase)
# =========================
# 중요: Supabase Dashboard > Settings > Database > Connection string 에서 복사
# Project ID, Region, Pooler 주소는 Dashboard에서 직접 확인하세요.

# Transaction pooler (포트 6543) - 일반 쿼리용
# pgbouncer=true 필수!
DATABASE_URL="postgresql://postgres.YOUR_PROJECT_REF:[YOUR-PASSWORD]@YOUR_POOLER_HOST:6543/postgres?pgbouncer=true"

# Session pooler (포트 5432) - Migration/Introspection용
DIRECT_URL="postgresql://postgres.YOUR_PROJECT_REF:[YOUR-PASSWORD]@YOUR_POOLER_HOST:5432/postgres"

# =========================
# JWT 인증 설정
# =========================
JWT_SECRET=your-jwt-secret-change-in-production
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# =========================
# CORS 설정
# =========================
# 여러 origin은 쉼표로 구분
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
CORS_CREDENTIALS=true
