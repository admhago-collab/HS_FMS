{
  "name": "harness-mes",
  "version": "0.1.0",
  "private": true,
  "description": "HARNESS MES - Manufacturing Execution System for Wire Harness Production",
  "scripts": {
    "dev": "turbo dev",
    "kill": "taskkill /F /FI \"WINDOWTITLE eq turbo\" >nul 2>&1 & (FOR /F \"tokens=5\" %P IN ('netstat -ano ^| findstr :3002 ^| findstr LISTENING') DO taskkill /F /PID %P >nul 2>&1) & (FOR /F \"tokens=5\" %P IN ('netstat -ano ^| findstr :3003 ^| findstr LISTENING') DO taskkill /F /PID %P >nul 2>&1) & echo Killed dev servers",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean && rimraf node_modules",
    "db:generate": "turbo db:generate",
    "db:push": "turbo db:push",
    "db:pull": "turbo db:pull",
    "db:seed": "turbo db:seed"
  },
  "devDependencies": {
    "docx": "^9.6.1",
    "rimraf": "^6.0.1",
    "turbo": "^2.4.4",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "unrs-resolver"
    ]
  }
}
